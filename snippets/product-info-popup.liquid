 {% if section.settings.show_popup %}
<!-- Trigger/Open The Modal -->
<div id="myBtn">ⓘ</div>

<!-- The Modal -->
<div id="myModal" class="modal">

  <!-- Modal content -->
  <div class="modal-content modal__container  price-info">
    <i class="close-icon modal-close close"></i>
    
     <div class="price-info__info">
       {% if section.settings.title != blank %}
                 <h3 class="hd2"> {{ section.settings.title }}</h3>
                  {% endif %}
           {% if section.settings.text != blank %}
          <p>{{ section.settings.text }}</p>
        {% endif %}
            
        </div>
    {% if section.settings.title != blank %}
        <div class="price-info__img">
          <img class=" lazyloaded" src="{{section.settings.image | img_url:'master'}}" alt="{{ shop.name }}" itemprop="logo">
        </div>
    {% endif %}
       
  </div>

</div>
{% endif %}

<script>
// Get the modal
var modal = document.getElementById("myModal");

// Get the button that opens the modal
var btn = document.getElementById("myBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks the button, open the modal 
btn.onclick = function() {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
</script>
